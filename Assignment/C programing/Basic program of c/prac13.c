//Find character value from ascii.
#include <stdio.h>
int main() {
   char ch ;
    printf("Enter the ASCII value: ");
    scanf("%c", &ch);
    printf("The character corresponding to the ASCII value '%c' is: %d\n",ch, ch);
}
